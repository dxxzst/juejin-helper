name: Auto

on:
  schedule:
    - cron: "30 22 * * *" # 北京时间上午06:30
  workflow_dispatch:

env:
  COOKIE: ${{ secrets.COOKIE }}
  COOKIE_2: ${{ secrets.COOKIE_2 }}
  COOKIE_3: ${{ secrets.COOKIE_3 }}
  COOKIE_4: ${{ secrets.COOKIE_4 }}
  COOKIE_5: ${{ secrets.COOKIE_5 }}
  EMAIL_USER: ${{ secrets.EMAIL_USER }}
  EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
  EMAIL_TO: ${{ secrets.EMAIL_TO }}
  DINGDING_WEBHOOK: ${{ secrets.DINGDING_WEBHOOK }}
  PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
  WEIXIN_WEBHOOK: ${{ secrets.WEIXIN_WEBHOOK }}
  SERVERPUSHKEY: ${{ secrets.SERVERPUSHKEY }}
  FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}

jobs:
  CheckIn:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2

      - name: Run Project with Retry
        run: |
          cd workflows

          echo "📦 安装依赖（带重试）"
          for i in 1 2 3; do
            yarn && break
            echo "❗ yarn 安装失败，第 $i 次重试，等待 120 秒..."
            sleep 120
          done

          echo "🚀 执行 checkin（带重试）"
          for i in 1 2 3; do
            yarn checkin && break
            echo "❗ checkin 执行失败，第 $i 次重试，等待 120 秒..."
            sleep 120
          done

#  SeaGold:
#    needs: CheckIn
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-node@v2
#      - name: Run Project with Retry
#        run: |
#          cd workflows
#
#          echo "📦 安装依赖（带重试）"
#          for i in 1 2 3; do
#            yarn && break
#            echo "❗ yarn 安装失败，第 $i 次重试，等待 120 秒..."
#            sleep 120
#          done
#
#          echo "🚀 执行 seagold（带重试）"
#          for i in 1 2 3; do
#            yarn seagold && break
#            echo "❗ seagold 执行失败，第 $i 次重试，等待 120 秒..."
#            sleep 120
#          done
